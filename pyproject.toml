[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-sc"
version = "0.1.0"
description = "SuperCollider ワークフロー用 MCP サーバー (Python)"
requires-python = ">=3.10"

[project.scripts]
mcp-sc = "mcp_sc:main"

[tool.setuptools]
packages = ["plugins"]